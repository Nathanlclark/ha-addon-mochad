---
configuration:
  log_level:
    name: Log level
    description: The amount of logging the add-on should do.
  port_number:
    name: Port Number to use
  device_bus_path:
    name: USB Bus for the device (eg /dev/bus/usb/002)
    description: Use lsusb to identify the bus of the device